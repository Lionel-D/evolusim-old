{% extends 'globals/header.html.twig' %}

{% block header_nav %}
    <li class="nav-item{% if app.request.get('_route') == 'home' %} active{% endif %}">
        <a href="{{ path('home') }}" class="nav-link">
            Home
        </a>
    </li>
    <li class="nav-item dropdown{% if 'simulation_' in app.request.get('_route') %} active{% endif %}">
        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            Simulation
        </a>
        <ul class="dropdown-menu">
            <li>
                <a href="{{ path('simulation_index') }}" class="dropdown-item{% if app.request.get('_route') == 'simulation_index' %} active{% endif %}">Index</a>
            </li>
            <li>
                <a href="{{ path('simulation_index') }}" class="dropdown-item{% if app.request.get('_route') == 'simulation_new' %} active{% endif %}">Create new</a>
            </li>
        </ul>
    </li>
{% endblock %}